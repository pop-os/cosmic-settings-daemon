[package]
name = "cosmic-settings-daemon-config"
version = "0.1.0"
edition = "2024"

[dependencies]
cosmic-config.workspace = true
serde = { version = "1.0", features = ["derive"] }
cosmic-theme.workspace = true
cosmic-theme.optional = true

[features]
default = []
greeter = ["cosmic-theme"]
