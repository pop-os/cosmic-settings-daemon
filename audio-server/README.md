# cosmic-settings-audio-server

Implements the `com.system76.CosmicSettings.Audio` varlink interface for the `com.system76.CosmicSettings` varlink service. This allows clients to interact with audio node and application volumes over varlink, manage device profiles and routes, and to watch audio device stats in realtime.
